import { FC, ReactElement } from "react";

const Customer: FC = (): ReactElement => {
  return (
    <h2 className="text-xl font-bold mb-6 ">👋 Welcome, Rahul Karadwal</h2>
  );
};

export default Customer;
